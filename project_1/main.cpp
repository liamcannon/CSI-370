#include <iostream>
#include <sys/socket.h>
#define port 8080

using namespace std;

int main(int argc, char const *arvg[]) {
    // asm ("movl $42, %%eax \n\t "
    // "inc %%eax"
    // :
    // :
    // :   "%eax"
    // );
    int server_sock, new_sock, val;
    
    return 0;
}